#include<bits/stdc++.h>
using namespace std;

// Aim- Given an IP address schema, implement a program to print for the following-
    // 1. Identify the class of IPv4 in binary notation.
    // 2. Generate the newtwork address, network id, host id for class IPv4 address using default masking value
    // 3. Generate the 1st address, last addr and total no. of add for class IPv4 schema
    // 4. Calculation of custom masking values for classless IPv4 address

void deciToBinary(int n){
    vector <int> binaryNum;

    int i = 0;
    while(n>0){
        binaryNum.push_back( n % 2 );
        n = n/2;
        i+f32addf128+;
    }
    for(auto x: binaryNum){
        cout<<x;
    }
}

void binaryToDeci(int n){
    vector <int> 
}

void classInBinary(){

}

int main(){
    deciToBinary(128);

    cout<<endl;
    return 0;
}